# Issue #20: (12) Asset kind

- URL: https://github.com/hemuu817/TakuVault/issues/20
- State: OPEN
- UpdatedAt: 2026-01-12T16:14:53Z
- Labels: 

## Body

# #20 Asset.kind（ADR-0002 実装）

## 目的（このIssueで達成する価値）
ADR-0002で固定した `assets.kind`（content_type自動判定）を実装し、一覧/未整理/検索で使える状態にする。

## スコープ
**含む**
- `assets.kind` の追加（enum/int、NOT NULL、default: other）
- 添付（has_one_attached :file）から kind を自動設定して保存
- リクエストで `kind` を送っても反映されない（改ざん不可）

**含まない**
- 手動編集UI（MVP外）
- 添付差し替え（MVP外）
- 許可/拒否（Content-Type/拡張子/サイズ上限）の仕様決定（別Issue）

## 受け入れ条件（Yes/No）
- [ ] Asset作成時、Blobの `content_type` に基づき kind が自動設定される（ADR-0002準拠）
- [ ] 添付なしではAssetを作成できない
- [ ] `assets.kind` は NOT NULL（default: other）で保存される
- [ ] `kind` パラメータを送っても値は上書きできない
- [ ] structure.sql に反映されている

## DoD（完了条件）
- [ ] migration追加（kind列、NOT NULL、default）
- [ ] 判定ロジックがモデル/サービスのどちらかに集約されている
- [ ] 最小テストが通る（下記）

## 失敗ケース（最低3つ）
- [ ] `kind` をparamsで改ざんできる
- [ ] `image/png` が other になる（判定漏れ）
- [ ] 添付なしで保存できてしまう（kind前提が崩れる）

## 最小テスト/検証
**Model/DB**
- [ ] 添付必須
- [ ] image/png→image、audio/mpeg→audio、video/mp4→video、application/pdf→other
- [ ] kind NOT NULL + default other

**Request/System**
- [ ] `kind=video` を送っても Blob content_type に従う（改ざん不可）

## Depends on
- Depends on: Epic D（Asset作成導線：1ファイル=1Asset、複数投入）
- Depends on: アップロード安全性（許可Content-Type/拡張子/サイズ上限：Epic D/X）

## Relates to
- Relates to: ADR-0002（本ADR）
- Relates to: Epic E（kind絞り込み）

