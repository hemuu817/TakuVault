# Issue #20: (15) Asset kind

- URL: https://github.com/hemuu817/TakuVault/issues/20
- State: OPEN
- UpdatedAt: 2026-07-03T08:00:10Z
- Labels: 

## Body

# #20 Asset.kind

## 目的（このIssueで達成する価値）
ADR-0017で固定した `assets.kind`（content_type自動判定）を実装し、一覧/未整理/検索等で利用できる分類軸として保存できる状態にする。

## スコープ
**含む**
- `assets.kind` の追加（enum/int、NOT NULL、default: other）
- Asset.kind の値は `other`, `image`, `audio` に固定する
- `video` は定義しない
- 添付（has_one_attached :file）から kind を自動設定して保存
- リクエストで `kind` を送っても反映されない（改ざん不可）
- 既存Assetがある場合、migration時に `kind` が NULL のまま残らないようにする

**含まない**
- 手動編集UI（対象外）
- 添付差し替え（対象外）
- 許可/拒否（Content-Type/拡張子/サイズ上限）の仕様決定（別Issue/ADR）
- Usage.role の追加
- `Usage::DISPLAY_ROLE_ORDER` の追加・更新
- 未整理（Usage0）ページでの kind 連動UI補助
- Asset.kind から Usage.role を自動決定する処理
- kind-role 不一致をサーバ側で拒否する制約
- Asset一覧全体の検索・絞り込み・タグ機能

## 受け入れ条件（Yes/No）
- [ ] Asset作成時、Blobの `content_type` に基づき kind が自動設定される（ADR-0017準拠）
- [ ] `image/png` または `image/jpeg` が `image` になる
- [ ] `audio/mpeg` または `audio/wav` が `audio` になる
- [ ] 判定対象外または不明な `content_type` は `other` になる
- [ ] 添付なしではAssetを作成できない
- [ ] `assets.kind` は NOT NULL（default: other）で保存される
- [ ] Asset.kind の enum は `other: 0`, `image: 1`, `audio: 2` で定義されている
- [ ] `video` が Asset.kind に定義されていない
- [ ] `kind` パラメータを送っても値は上書きできない
- [ ] 既存Assetがある場合、migration後に `kind` が NULL のAssetが残らない
- [ ] structure.sql に反映されている

## DoD（完了条件）
- [ ] migration追加（kind列、NOT NULL、default）
- [ ] 既存Asset向けのバックフィルが必要な場合は実施されている
- [ ] Asset.kind の enum が ADR-0017 と一致している
- [ ] 判定ロジックがモデル/サービスのどちらかに集約されている
- [ ] `kind` params による改ざんができない
- [ ] 最小テストが通る（下記）

## 失敗ケース（最低3つ）
- [ ] `kind` をparamsで改ざんできる
- [ ] `image/png` が other になる（判定漏れ）
- [ ] `audio/mpeg` が other になる（判定漏れ）
- [ ] 添付なしで保存できてしまう（kind前提が崩れる）
- [ ] `assets.kind` に NULL が保存される
- [ ] `video` enum が残っている

## 最小テスト/検証
**Model/DB**
- [ ] 添付必須
- [ ] image/png → image
- [ ] image/jpeg → image
- [ ] audio/mpeg → audio
- [ ] audio/wav → audio
- [ ] 判定対象外または不明な content_type → other
- [ ] kind NOT NULL + default other
- [ ] enum が `other: 0`, `image: 1`, `audio: 2`
- [ ] `video` が enum に存在しない
- [ ] kind params で上書きできない

## Depends on
- Depends on: Epic D（Asset作成導線：1ファイル=1Asset、複数投入）
- Depends on: アップロード安全性（許可Content-Type/拡張子/サイズ上限：Epic D/X）

## Relates to
- ~~Relates to: ADR-0002（本ADR）~~
  - superseded
- Relates to: ADR-0017（Asset.kind）
- Relates to: ADR-0014（許可形式・判定方式・エラーハンドリング）

