# Issue #8: (3) 初回デプロイ(staging)

- URL: https://github.com/hemuu817/TakuVault/issues/8
- State: OPEN
- UpdatedAt: 2026-01-12T09:28:03Z
- Labels: relate-to-X

## Body

## 目的
stagingで「動作確認できる定義」を固定し、以降の機能実装をstagingで検証可能にする。  
R2はここでは実装しない（永続ストレージはEpic Xへ）。

## 受入条件（AC）
- [ ] Render（Docker）でデプロイが成功する
- [ ] DB接続・migrateが通る
- [ ] トップページが表示できる
- [ ] `GET /up`（または同等）が200で返る（ヘルスチェックの基準が確立）

## 作業（チェックリスト）
- [ ] Render用の起動コマンド / Procfile / entrypoint 整理（構成はプロジェクト方針に合わせる）
- [ ] `RAILS_ENV=production` 相当での起動確認（staging設定）
- [ ] `/up` 追加（ルーティング + コントローラ等。アプリの最小死活確認）
- [ ] migrate実行手順を固定（Renderの設定に記載）

## 注意（ここでやらない）
- [ ] R2接続（ActiveStorageの永続化）はEpic Xへ
- [ ] stagingでの添付永続性は担保しない（RenderのFS前提をREADMEに明記）

## DoD
- [ ] stagingでトップ表示
- [ ] ヘルスチェック200

## Xとの紐付け
- Relates to: **Epic X-21（本番ストレージ構成：R2）**
  - 理由：stagingはディスクで最短確認、本番永続はXでR2に切替

